

## frame

Horizontal Frame

Provides the basis for horizontal visualizations where the y axis grows to the right (ie. bar chart)

Example:
    new Narwhal(config)
       .cartesian()
       .horizontal()
       .bar([1,2,3])
       .render();

