#### **rotatedFrame** : {<%= type %>}

<% if(defaultValue !== "[object Object]") { %>*default: <%= defaultValue %>* <% }%>

Whether or not the visualization has been rotated.

**Notes:** Read only.

<% if(notes) { %><%= notes %><% } %>

