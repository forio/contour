#### **stacked** : {<%= type %>}

This is the stacked property of the area chart.

<% if(notes) { %><%= notes %><% } %>
<% if(defaultValue !== "[object Object]") { %>*default: <%= defaultValue %>* <% }%>
